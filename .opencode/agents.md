# Contesto Progetto: Water Jug Puzzle BFS
- **Stato Attuale:** La logica core (`solver.py` e `models.py`) e i test (`test_solver.py`) sono completati e funzionanti. L'algoritmo BFS e il check GCD sono corretti.
- **Prossimo Obiettivo:** Creare l'interfaccia grafica usando `streamlit`. 
- **Requisiti GUI:** Deve mostrare i vasi visivamente, permettere l'input di capacità/target, e mostrare la lista dei passaggi. L'utilizzo di `graphviz` per l'albero BFS è opzionale al momento, priorità alla UI base pulita e funzionale.